# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
dist-standalone
*.local

# Auth config (argon2id hash) — generated locally with generate_hash, never committed
auth.hash

# KMS activation config — editable without recompiling, environment-specific
kms.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
legacy_code/*
bats-funcionalidades/
